---
"@ayulab/pi-rewind": patch
---

Skip Rewind and tree file-sync prompts when the target conversation point already matches the currently synced code checkpoint, while prompting again after code has been restored to an older checkpoint.
