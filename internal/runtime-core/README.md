# @ayulab/runtime-core

Shared zero-dependency utilities for Ayu Pi packages.

This package contains small runtime helpers that also provide TypeScript narrowing, such as `isRecord`, `isArrayOf`, `errorMessage`, and non-empty array guards. It intentionally stays independent from extension or checkpoint-specific concepts.
